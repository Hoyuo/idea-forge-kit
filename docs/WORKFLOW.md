# IdeaForge 워크플로우 다이어그램

## 전체 흐름

```mermaid
flowchart TB
    subgraph Phase1["Phase 1: Ideate"]
        A[/"💡 아이디어 입력"/] --> B["🤖 AI 질문"]
        B --> C["📝 PRD 자동 생성"]
        C --> D[("📄 .forge/prds/XXX-001.md")]
    end

    subgraph Phase2["Phase 2: Analyze"]
        D --> E["🔍 PRD 분석"]
        E --> F["🎯 도메인 식별"]
        F --> G["🤖 에이전트 자동 생성"]
        G --> H["📋 태스크 분해"]
        H --> I[("📁 .forge/tasks/XXX-001/")]
    end

    subgraph Phase3["Phase 3: Build"]
        I --> J["🔴 RED: 테스트 작성"]
        J --> K["🟢 GREEN: 구현"]
        K --> L["🔵 REFACTOR: 개선"]
        L --> M{{"✅ 태스크 완료?"}}
        M -->|No| J
        M -->|Yes| N{{"📋 다음 태스크?"}}
        N -->|Yes| J
        N -->|No| O["🎉 빌드 완료"]
    end

    subgraph Phase4["Phase 4: Verify"]
        O --> P["✔️ 요구사항 검증"]
        P --> Q["📊 테스트 커버리지"]
        Q --> R["📈 품질 검사"]
        R --> S["📋 최종 리포트"]
    end

    style Phase1 fill:#e1f5fe
    style Phase2 fill:#fff3e0
    style Phase3 fill:#e8f5e9
    style Phase4 fill:#f3e5f5
```

## 명령어 흐름

```mermaid
flowchart LR
    subgraph Commands["🎮 슬래시 명령어"]
        C1["/forge:idea"]
        C2["/forge:analyze"]
        C3["/forge:build"]
        C4["/forge:verify"]
        C5["/forge:status"]
        C6["/forge:resume"]
    end

    C1 -->|"PRD 생성"| C2
    C2 -->|"에이전트 생성"| C3
    C3 -->|"TDD 구현"| C4
    C4 -->|"검증 완료"| Done["✅ 완료"]

    C5 -.->|"진행 상황"| C3
    C6 -.->|"재개"| C3

    style C1 fill:#bbdefb
    style C2 fill:#ffe0b2
    style C3 fill:#c8e6c9
    style C4 fill:#e1bee7
```

## 에이전트 자동 생성 로직

```mermaid
flowchart TB
    subgraph Input["📥 입력"]
        PRD["PRD 문서"]
    end

    subgraph Analysis["🔍 분석"]
        PRD --> Domains["도메인 추출"]
        Domains --> Keywords["키워드 분석"]
        Keywords --> Tech["기술 스택 식별"]
    end

    subgraph Mapping["🗺️ 매핑"]
        Tech --> Backend{{"Backend?"}}
        Tech --> Frontend{{"Frontend?"}}
        Tech --> Database{{"Database?"}}
        Tech --> Security{{"Security?"}}
        Tech --> DevOps{{"DevOps?"}}
    end

    subgraph Generation["🤖 생성"]
        Backend -->|Yes| AgentBackend["expert-backend.md"]
        Frontend -->|Yes| AgentFrontend["expert-frontend.md"]
        Database -->|Yes| AgentDB["expert-database.md"]
        Security -->|Yes| AgentSec["expert-security.md"]
        DevOps -->|Yes| AgentOps["expert-devops.md"]
    end

    style Input fill:#e3f2fd
    style Analysis fill:#fff8e1
    style Mapping fill:#f3e5f5
    style Generation fill:#e8f5e9
```

## TDD 사이클 상세

```mermaid
stateDiagram-v2
    [*] --> RED

    RED: 🔴 RED
    note right of RED
        테스트 작성
        실패 확인
    end note

    GREEN: 🟢 GREEN
    note right of GREEN
        최소 구현
        테스트 통과
    end note

    REFACTOR: 🔵 REFACTOR
    note right of REFACTOR
        코드 개선
        테스트 유지
    end note

    CHECK: ✅ 체크포인트
    note right of CHECK
        진행 상황 저장
        다음 태스크 확인
    end note

    RED --> GREEN: 테스트 통과
    GREEN --> REFACTOR: 통과 확인
    REFACTOR --> CHECK: 완료
    CHECK --> RED: 다음 태스크
    CHECK --> [*]: 모든 태스크 완료
```

## 파일 구조

```
project/
├── .claude/
│   ├── agents/
│   │   ├── forge-orchestrator.md    # 메인 오케스트레이터
│   │   ├── forge-prd-writer.md      # PRD 작성 전문가
│   │   ├── forge-analyzer.md        # 요구사항 분석가
│   │   └── forge-tdd-runner.md      # TDD 실행자
│   │
│   ├── commands/forge/
│   │   ├── idea.md                  # /forge:idea
│   │   ├── analyze.md               # /forge:analyze
│   │   ├── build.md                 # /forge:build
│   │   ├── verify.md                # /forge:verify
│   │   ├── status.md                # /forge:status
│   │   ├── list.md                  # /forge:list
│   │   └── resume.md                # /forge:resume
│   │
│   ├── skills/forge-patterns/
│   │   └── SKILL.md                 # 코딩 패턴
│   │
│   └── settings.json                # 권한 설정
│
├── .forge/
│   ├── config.json                  # IdeaForge 설정
│   │
│   ├── prds/                        # PRD 문서들
│   │   ├── AUTH-001.md
│   │   ├── CHAT-002.md
│   │   └── ...
│   │
│   ├── tasks/                       # 태스크 분해
│   │   ├── AUTH-001/
│   │   │   ├── tasks.json           # 태스크 목록
│   │   │   └── breakdown.md         # 분해 내용
│   │   └── ...
│   │
│   ├── agents/                      # 동적 생성 에이전트
│   │   ├── AUTH-001/
│   │   │   ├── expert-backend.md
│   │   │   └── expert-security.md
│   │   └── ...
│   │
│   ├── progress/                    # 진행 상황
│   │   ├── AUTH-001/
│   │   │   ├── checkpoint.json      # 체크포인트
│   │   │   └── history.json         # 작업 이력
│   │   └── ...
│   │
│   └── reports/                     # 최종 리포트
│       ├── AUTH-001-final.md
│       └── ...
│
├── .mcp.json                        # MCP 서버 설정
├── CLAUDE.md                        # 프로젝트 지시문
└── README.md
```

## 진행 상황 추적 UI

```
┌─────────────────────────────────────────────────────────────────┐
│  🔨 IdeaForge v0.1.0                              📊 Dashboard   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📋 Current PRD: AUTH-001 (사용자 인증 시스템)                    │
│  ────────────────────────────────────────────────────────────── │
│                                                                 │
│  Phase: [■] Idea  [■] Analyze  [▶] Build  [ ] Verify            │
│                                                                 │
│  ┌─ Requirements ─────────────────────────────────────────────┐ │
│  │ ✅ FR-001: 이메일/비밀번호 로그인                            │ │
│  │ 🔄 FR-002: OAuth 소셜 로그인 (진행 중)                       │ │
│  │ ⬚ FR-003: 비밀번호 재설정                                   │ │
│  │ ⬚ NFR-001: 응답시간 < 200ms                                │ │
│  │ ⬚ NFR-002: 보안 표준 준수                                   │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ TDD Cycle ────────────────────────────────────────────────┐ │
│  │                                                             │ │
│  │   FR-002 OAuth 로그인                                       │ │
│  │   [🔴 RED] ──▶ [🟢 GREEN] ──▶ [🔵 REFACTOR]                │ │
│  │      ✓          진행 중            대기                      │ │
│  │                                                             │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ Active Agents ────────────────────────────────────────────┐ │
│  │ 🤖 expert-backend     │ OAuth 토큰 검증 로직 구현 중         │ │
│  │ 💤 expert-security    │ 대기 중                             │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Progress: ████████████░░░░░░░░░░░░░░░░░░░ 40%  (2/5 tasks)    │
│                                                                 │
│  ⏱️ Elapsed: 1h 23m  │  📈 Est. Remaining: 2h 10m               │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  [/forge:resume] [/forge:status] [/forge:pause] [/forge:verify] │
└─────────────────────────────────────────────────────────────────┘
```
